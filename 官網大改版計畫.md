# 杉蔓官網 2.0 大改版計畫

> 版本：v1（✅ 已確認 2026-07-11）｜對象：www.sustainablemans.com
> 已確認決策：視覺方案 A（深色雙翼 Supabase綠×Linear紫）；數位翼文案由 AI 草擬後修改；
> 路由 `/green/` + `/digital/`（主網域底下）；每週 AI 產文管線納入本次範圍（Phase 3）
> 現行版本：v1.0-stable（Astro 6 + Tailwind 4 + Vercel + Decap CMS）

---

## 〇、改版總覽（一張圖）

```
                    ┌─────────────────────────────┐
                    │      / （新入口動畫頁）        │
                    │  ┌──────────┬──────────┐   │
                    │  │ 永續轉型  │  數位轉型  │   │
                    │  │ 碳管理·節能│ AI導入·數位 │   │
                    │  │ ·綠色印刷 │ 建置·自動化 │   │
                    │  └────┬─────┴─────┬────┘   │
                    └───────┼───────────┼────────┘
                ┌───────────▼──┐   ┌────▼──────────┐
                │ /green/ 永續翼 │◄─►│ /digital/ 數位翼│  ← 頂部可互切
                │ 深色×翡翠綠    │   │ 深色×科技藍紫   │
                │ (Supabase風格) │   │ (Linear風格)   │
                ├──────────────┤   ├───────────────┤
                │ MS-RDK 節能   │   │ AI 導入顧問     │
                │ ISO 14064/67 │   │ 數位建置(網站/系統)│
                │ ESG 桌遊     │   │ 企業自動化       │
                │ 綠色策展印刷   │   │                │
                └──────┬───────┘   └───────┬───────┘
                       └───── 共用 ────────┘
                  關於我們 / 知識庫 / 聯絡 / 法規頁
```

---

## 一、Phase 0：錯誤修復（立即執行，改版前先上線）

| # | 問題 | 修法 |
|---|---|---|
| 0-1 | canonical / og:url / og:image / JSON-LD 全指向舊網域 `sustainable-man.vercel.app` | `Layout.astro` 改用 `https://www.sustainablemans.com`，並改為**每頁自動生成** canonical（`Astro.url`），不再寫死首頁網址 |
| 0-2 | `site.json` 聯絡資訊是佔位符死資料；`ContactForm.astro` 硬編碼真值 | `site.json` 填入真值（sustainablemans@gmail.com / 0971-007-051 / 高雄市楠梓區秀群路499巷28號），`ContactForm.astro` 改讀 `site.json` — 之後從後台改一處就全站生效 |
| 0-3 | 頁尾版權 `© 2024` | 改為動態年份（build 時自動） |
| 0-4 | **CMS 服務欄位缺漏（資料損毀陷阱）**：`admin/index.html` 內嵌設定缺 `stats`、`image`、`isFlagship` 欄位，從後台存檔會刪掉這些資料 | 補齊 CMS 欄位定義（與 Phase 4 一併重整） |

**產出**：一次 commit 部署，Phase 0 完成即可獨立上線，不等改版。

---

## 二、Phase 1：資訊架構重整 + 入口動畫頁

### 1A. 新資訊架構

| 路由 | 內容 | 備註 |
|---|---|---|
| `/` | **入口動畫頁**（雙門戶） | 新增 |
| `/green/` | 永續轉型首頁（現有首頁內容進化版） | 現有內容遷移 |
| `/green/services/[slug]` | 永續服務內頁 ×4~5 | 現有 5 服務歸此翼 |
| `/digital/` | 數位轉型首頁 | **全新** |
| `/digital/services/[slug]` | 數位服務內頁 ×3 | **全新內容** |
| `/about/`、`/knowledge/`、`/contact/`、`/impact/`、`/privacy/`、`/terms/` | 共用頁 | 保留，重新套皮 |
| 舊網址 `/services/*` | 301 轉址到 `/green/services/*` | SEO 權重不流失 |

### 1B. 入口動畫頁規格

- **版面**：左右分屏（desktop）／上下堆疊（mobile）
  - 左：**永續轉型** ＋ 小字「碳管理 · 節能 · 綠色印刷」→ 進入 `/green/`
  - 右：**數位轉型** ＋ 小字「AI導入 · 數位建置 · 企業自動化」→ 進入 `/digital/`
- **動畫**：
  - 進場：品牌 Logo 中央浮現 → 分屏從中線向兩側展開（stagger reveal）
  - Hover：滑入側擴張至 ~62%、另一側收縮變暗，背景微粒子/光暈流動
  - 點擊：該側全屏擴張過場（View Transitions）進入對應翼
- **互切機制**：兩翼的 Header 常駐一個「翼切換器」（永續 ⇄ 數位 膠囊開關），
  切換時以顏色主題交叉淡變過場；記憶使用者上次選擇（localStorage），
  回訪時 `/` 可提示「繼續上次瀏覽」
- **SEO 注意**：入口頁仍需完整 title/description/JSON-LD，且提供兩翼的
  文字連結（不能只有動畫按鈕），確保爬蟲可達

### 1C. 數位轉型翼 — 新服務內容（草稿，需你確認/補充）

| 服務 | 草擬定位 |
|---|---|
| **AI 導入顧問** | 企業 AI 評估與導入：流程盤點、AI 工具選型、教育訓練（可結合你們既有的顧問與課程能量） |
| **數位建置** | 官網/形象網站/系統建置（正好以杉蔓官網 2.0 為作品案例） |
| **企業自動化** | 工作流自動化：報表、行政流程、客服/行銷自動化 |

> ⚠️ 這是全新業務線，文案細節、定價邏輯、實績案例需要你提供素材，我可先草擬全部文案供你修改。

---

## 三、Phase 2：視覺大改版（雙翼風格系統）

### 推薦方案 A：「同底盤、雙靈魂」（推薦 ★）

以已安裝的 design-md 風格庫為基準，兩翼共用同一套版面系統（字階、間距、元件），只換色彩靈魂：

| | 永續翼 `/green/` | 數位翼 `/digital/` |
|---|---|---|
| 參考風格 | **Supabase**（深色 × 翡翠綠） | **Linear**（極黑 × 薰衣草藍紫 #5e6ad2） |
| 品牌延續 | 延續現有 emerald 識別，客戶不陌生 | 全新科技感，與永續翼形成對比 |
| 質感方向 | 有機、資料驅動、生態 | 精密、軟體工藝、自動化 |

- 入口頁左綠右紫，視覺敘事直觀
- 共用元件只需維護一套，用 CSS variables 換膚（`data-wing="green|digital"`）
- 相較現在的深綠漸層+霓虹黃，整體更收斂、更高級（去掉大量 uppercase 斜體、
  過度 tracking，改用系統化的字階與 hairline 邊框）

### 備選方案 B：「整站亮色高級感」

- 參考 **Stripe**：亮底、精緻漸層點綴、兩翼用強調色區分（綠/藍紫）
- 優點：資訊可讀性最高、B2B 信任感強；缺點：與現有品牌視覺斷裂最大

### 改版範圍（不論選 A/B）

- 全站排版系統重寫（`global.css` @theme tokens 依 DESIGN.md 重建）
- Header/Footer/Hero/ServiceCard/TeamCarousel 等 14 個組件重新設計
- 動效升級：維持 stagger reveal + View Transitions，新增翼主題過場
- 圖片方向：現有 AI 形象圖沿用可用者，不足處補生成（配新色調）

---

## 四、Phase 3：SEO + GEO 全面導入（含自動更新）

### 4A. 技術 SEO（build 時自動生成，永不過期）

| 項目 | 做法 |
|---|---|
| Sitemap | `@astrojs/sitemap` — 每次 build 自動重生 `sitemap-index.xml` |
| robots.txt | 靜態產出，指向 sitemap；`/admin` 禁爬 |
| Canonical | 每頁由 `Astro.url` 自動生成（Phase 0 已修） |
| Meta/OG | 每頁獨立 title/description/og:image；文章頁用文章首圖 |
| 結構化資料 | 分頁型別注入：`Organization`+`LocalBusiness`（含高雄地址、電話→本地搜尋）、`Service`（每個服務內頁）、`Article`（知識庫）、`BreadcrumbList`、`FAQPage`（服務頁加 FAQ 區塊） |
| 效能 | 圖片轉 WebP + `astro:assets` 自動優化、lazy loading（Core Web Vitals 直接影響排名） |
| 301 轉址 | 舊 `/services/*` → `/green/services/*`（`vercel.json` redirects） |

### 4B. GEO（生成式引擎優化 — 讓 ChatGPT/Gemini/Perplexity 引用你們）

| 項目 | 做法 |
|---|---|
| `llms.txt` | build 時從 content collections **自動生成**：公司簡介 + 全部服務 + 文章索引（AI 爬蟲的官方入口） |
| `llms-full.txt` | 全站內容純文字完整版，自動生成 |
| 文章 GEO 化 | 知識庫文章補「問答式小標 + 重點摘要框 + 數據引用」格式（AI 最愛引用的結構） |
| FAQ Schema | 每個服務頁 3-5 題 FAQ（同時餵 Google 精選摘要與 AI 引擎） |
| RSS Feed | `/rss.xml` 自動生成，加速內容被發現 |

### 4C. 自動更新機制

```
後台編輯內容 → git commit → Vercel 自動 build
   └→ sitemap / llms.txt / RSS / JSON-LD 全部自動重生，零手動維護
```

另可加選（Phase 3+，可後議）：排程 AI 產文管線 — 每週自動草擬一篇
永續/數位趨勢文章存為 CMS 草稿，你審核後一鍵發布。

### 4D. 需要你操作的一次性設定

- Google Search Console 驗證 + 提交 sitemap（我出步驟教學）
- Bing Webmaster Tools 同上（Bing 餵 ChatGPT 搜尋）

---

## 五、Phase 4：後台（CMS）易用性優化

### 現況痛點
Decap CMS 介面老舊、每次都要 GitHub OAuth 登入、服務欄位缺漏會毀資料。

### 方案：換 **Sveltia CMS**（Decap 的現代化直接替代品）

- **同一份設定檔直接沿用**，替換一行 script 即完成遷移（風險極低）
- 介面現代、速度快、**原生繁中介面**、深色模式
- **本地編輯模式**：支援瀏覽器 File System API 直接編輯本機檔案，
  **不用登入 GitHub** 也能改
- 圖片上傳體驗大幅優於 Decap

### 加值：「一鍵開後台」桌面捷徑（給你手動用）

製作 `開啟官網後台.bat`：
1. 自動啟動本地 dev server（`npm run dev`）
2. 自動開瀏覽器進 `localhost:4321/admin`（Sveltia 本地模式）
3. 改完存檔 → 即時預覽 → 確認後一鍵（或我代為）commit+push 上線

### CMS 設定重整

- 補齊服務欄位（stats/image/isFlagship）修復資料損毀陷阱
- 新增「數位服務」collection（雙翼分開管理）
- 每個欄位加中文提示與範例值
- 文章編輯器啟用即時預覽

---

## 六、執行順序與里程碑

| 階段 | 內容 | 預估工作量 | 上線點 |
|---|---|---|---|
| **Phase 0** | 錯誤修復 4 項 | 小（當天） | ✅ 立即部署 |
| **Phase 1** | 資訊架構 + 入口動畫頁 + 數位翼骨架與文案草稿 | 大 | 與 P2 一起上 |
| **Phase 2** | 雙翼視覺改版（14 組件 + tokens 重建） | 最大 | 🚀 2.0 主上線 |
| **Phase 3** | SEO/GEO 全套 + 自動化 | 中 | 隨 2.0 或緊接 |
| **Phase 4** | Sveltia CMS + 一鍵後台 + 欄位重整 | 中 | 可獨立上 |
| **驗收** | fresh-context subagent 驗收：全頁面實跑、Lighthouse、結構化資料驗證、後台實測 | — | 每階段各驗一次 |

### 安全網（回滾機制）

- 開工前打 git tag `v1.0-final`（現有 v1.0-stable 之後的最終狀態）
- Phase 1-2 在獨立 branch `redesign-2.0` 開發，Vercel Preview 網址先給你看，
  **你確認後才 merge 上正式站**
- 任何時刻可 `git reset --hard v1.0-final` 完全還原

---

## 七、待你確認的決策點

| # | 問題 | 我的建議 |
|---|---|---|
| 1 | 視覺走方案 A（深色雙翼：Supabase綠 × Linear紫）還是 B（亮色 Stripe）？ | **A** — 品牌延續性好、雙翼對比敘事強 |
| 2 | 數位轉型三項服務的文案：我先全部草擬你再改？還是你先給素材？ | 我先草擬 |
| 3 | 兩翼路由用 `/green/`+`/digital/`？或中文拼音/其他命名？ | `/green/` + `/digital/`（短、SEO 友善） |
| 4 | 週更 AI 產文管線（4C 加選）要不要納入本次範圍？ | 先不納入，2.0 穩定後再開 |

---

*確認後即建立任務清單開工，從 Phase 0 開始。*
