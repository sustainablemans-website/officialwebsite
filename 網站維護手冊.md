# 杉蔓官網 維護手冊

> 官網 2.0（雙翼版）的日常維護指南。給 Feba 的部分用白話寫，給工程／AI 的部分集中在後段。
> 最後更新：2026-07-12（2.0 改版上線版）

---

## 一、30 秒認識這個網站

| 項目 | 內容 |
|---|---|
| 正式網址 | https://www.sustainablemans.com |
| 架構 | 入口頁 → 永續翼 `/green/`（綠）⇄ 數位翼 `/digital/`（紫），共 26 頁 |
| 程式碼存放 | GitHub：`sustainablemans-website/officialwebsite`（`main` 分支 = 正式站） |
| 主機 | Vercel（push 到 main 就自動部署，1~3 分鐘上線） |
| 技術 | Astro 6 + Tailwind CSS 4 + Sveltia CMS 後台 |
| 本機專案位置 | `E:\網站設計\sustainable-mans-official` |

**核心觀念：所有內容都存在 GitHub 裡。** 不管從後台改、請 AI 改、還是手動改檔案，
最終都是「commit → push 到 main → Vercel 自動重建上線」。所以：
- 任何改壞的東西都能還原（Git 有完整歷史）
- sitemap、RSS、llms.txt、SEO 結構化資料**全部自動重生**，永遠不用手動維護

---

## 二、日常維護：想改什麼，去哪裡改？

| 我想… | 最推薦的方式 |
|---|---|
| 改文章 / 服務文案 / 團隊 / 活動 / 夥伴 / 聯絡資訊 | **後台**（見《後台使用說明.md》，或雙擊 `開啟官網後台.bat`） |
| 發新文章 | 後台 → 📰 知識專刊文章 → 新增 |
| 換圖片、加活動照片 | 後台上傳（⚡ 大圖請先壓縮，見下方「圖片守則」） |
| 改版面設計 / 加新頁面 / 改功能 | 請 AI 助手（Claude）處理，屬工程範圍 |
| 看網站流量與搜尋表現 | Google Search Console（設定方式見《SEO設定指南.md》） |

### 圖片守則（很重要，攸關網站速度）

2026-07 全站做過瘦身：39MB → 9.5MB，單頁載入從數 MB 降到 ~230KB。維持它的方法：

1. **單張圖片控制在 300KB 以下**，格式優先 WebP
2. 手機照片直接上傳前先壓縮，或上傳後請 AI 跑：
   `node scripts/optimize-images.mjs`（會自動轉 WebP + 縮尺寸）
3. 團隊成員照片目前有 3 張是 Google Drive 外連 → 建議改為後台上傳本站圖（待辦）

---

## 三、內容多久上線？出問題怎麼看？

```
後台儲存（或 push）→ GitHub main → Vercel 自動建置（1~3 分鐘）→ 正式站更新
```

- **超過 10 分鐘沒更新**：先 Ctrl+F5 強制重新整理；還是沒有 → 到 Vercel 後台看
  Deployments 是否有紅色 Failed，把錯誤截圖給 AI 助手
- **網站整個壞掉**：不要慌，跟 AI 說「還原到上一個正常版本」即可（見第五節）

---

## 四、自動化了哪些事（你完全不用管）

| 項目 | 說明 |
|---|---|
| sitemap-index.xml | 每次建置自動重生，Google 自動取得新頁面 |
| RSS（/rss.xml） | 新文章自動加入 |
| llms.txt / llms-full.txt | 給 ChatGPT/Perplexity 等 AI 引擎的官方內容摘要，自動同步全部服務與文章 |
| SEO 結構化資料 | 每頁 JSON-LD（公司資訊、服務、FAQ、文章、麵包屑）自動注入 |
| 版權年份 | 頁尾年份自動跟著今年 |
| 週更 AI 產文 | 每週自動草擬一篇永續/數位趨勢文章 → 存為**草稿**，你在後台審核、按發布才會上線（草稿絕不會自己上線） |

---

## 五、救援：改壞了怎麼還原？

所有歷史都在 Git，兩種還原：

1. **請 AI 幫忙（推薦）**：告訴 AI「大概什麼時候改壞的、哪個部分怪怪的」即可
2. **自己動手**（在專案資料夾開終端機）：
   ```bash
   git log --oneline -10        # 看最近 10 筆變更，找出壞掉前的那筆
   git revert <壞掉的commit>    # 產生一筆「反向修正」，安全不破壞歷史
   git push                     # 推上去，自動重新部署
   ```

重要還原點（git tag）：
- `v1.0-final` — 2.0 改版前的最後舊版
- `v1.0-stable-v1.0` — 更早的 1.0 穩定版

---

## 六、文件地圖（哪份文件管什麼）

| 文件 | 用途 |
|---|---|
| **網站維護手冊.md**（本文件） | 日常維護總覽、救援、自動化清單 |
| **後台使用說明.md** | 後台操作教學（線上 + 本機一鍵模式） |
| **SEO設定指南.md** | Google Search Console / Bing 一次性設定步驟 |
| 官網大改版計畫.md | 2.0 改版的完整規劃紀錄（歷史文件） |
| 官網設計與功能確認文件.md | 1.0 舊版盤點（歷史文件） |
| ~~UPDATE_GUIDE.md~~、~~詳細內容介紹.md~~ | ⚠️ 1.0 時代的舊文件，內容已過時，僅供考古 |

---

## 七、給工程／AI 的技術備忘

### 架構速記
- 路由：`src/pages/`——入口 `index.astro`；兩翼 `green/`、`digital/`（各含 `services/index.astro` + `services/[slug].astro`）；共用 about/knowledge/contact/impact/privacy/terms
- 雙翼換膚：`<html data-wing="green|digital">` + CSS variables（`--wing-primary` 等，定義於 `src/styles/global.css`）。永續翼 = Supabase 風（#3ecf8e），數位翼 = Linear 風（#5e6ad2）。共用頁預設 green
- 資料：`src/data/services.json`（永續 5 服務）、`digital-services.json`（數位 3 服務，schema 相同，含 faqs）、`site.json`（聯絡資訊單一資料源，ContactForm/Footer/schema 皆讀此）、`team/activities/partners.json`
- 文章：`src/content/articles/*.md`（Content Collections，schema 見 `src/content.config.ts`，支援 `draft: true`——draft 會被路由/列表/RSS/llms 全面過濾）
- JSON-LD：集中在 `src/lib/schema.ts`，經 Layout 的 `jsonLd` prop 注入
- 301 轉址：`vercel.json`（舊 `/services/*` → `/green/services/*`）

### 建置與部署
- `npm run build`；本機 OOM 時用 `NODE_OPTIONS=--max-old-space-size=4096`
- 部署：push main → Vercel 自動。**Vercel Preview 部署有登入保護**，分支 push 會產 Preview URL 供內部確認
- 圖片優化：`node scripts/optimize-images.mjs`（sharp；轉 WebP、按用途縮尺寸、可重跑）
- 已知坑：`getStaticPaths` 在模組層級執行，只能引用 import 的值，不能用 frontmatter const（曾造成整站建置失敗）

### CMS（詳見《後台使用說明.md》第六節）
- Sveltia CMS；`public/admin/config.yml` 欄位必須與 JSON/frontmatter schema 100% 對齊，**缺欄位＝後台存檔會刪資料**（曾發生，勿再犯）。新增資料欄位時，config.yml 要同步
- OAuth：`api/auth.js`、`api/callback.js`（Vercel serverless），環境變數 `OAUTH_CLIENT_ID` 等在 Vercel 後台

### SEO/GEO
- `astro.config.mjs` 已設 `site`；`@astrojs/sitemap` + `@astrojs/rss`
- `src/pages/llms.txt.ts`、`llms-full.txt.ts`、`rss.xml.ts` 為動態 endpoint，內容從 data/collections 組出，新增服務/文章會自動納入
- robots.txt：`Disallow: /admin/`

### 待辦（截至 2026-07-12）
- [ ] 上線後：GSC/Bing 提交（Feba 照《SEO設定指南.md》操作一次）
- [ ] 上線後：實測正式站 `/admin/` OAuth 登入流程
- [ ] 註冊週更 AI 產文排程（文章以 draft 進 CMS，Feba 審核發布）
- [ ] 團隊 3 張 Google Drive 外連照片改為本站資產
- [ ] 清理 repo 舊檔：根目錄舊版 `index.html`、`check_out.txt`、`favion.png`
