# 杉蔓官網 後台使用說明

> 給非工程師的白話版。官網後台已於 2026 年 7 月從 Decap CMS 升級為 **Sveltia CMS**：
> 介面更現代、速度更快、原生繁體中文，還多了「免登入的本機編輯模式」。

---

## 一、兩種使用方式，怎麼選？

| | 🌐 線上後台 | 💻 本機一鍵後台 |
|---|---|---|
| 怎麼開 | 瀏覽器打開網址即可 | 點兩下 `開啟官網後台.bat` |
| 需要登入嗎 | 要（GitHub 帳號） | **不用登入** |
| 存檔後 | 直接進入上線流程 | 只改本機檔案，需再推送上線 |
| 適合情境 | 改完就要上線的小修改 | 大量編輯、想先預覽再上線 |

---

## 二、🌐 線上後台

1. 打開網址：**https://www.sustainablemans.com/admin/**
2. 點「Sign in with GitHub」，用公司的 GitHub 帳號登入
   （帳號需有 `sustainablemans-website/officialwebsite` 這個倉庫的權限）
3. 左側選單挑選要編輯的內容 → 修改 → 按右上角「儲存」
4. 儲存後會自動 commit 到 GitHub 的 `main` 分支

**改了內容多久上線？**
儲存後 Vercel 會自動重新建置網站，一般 **1~3 分鐘**內就會出現在正式官網。
如果超過 10 分鐘還沒變，先按 Ctrl+F5 強制重新整理，再不行就找工程協助看 Vercel 的部署紀錄。

---

## 三、💻 本機一鍵後台（免登入）

> 需求：電腦已安裝 Node.js；瀏覽器請用 **Chrome 或 Edge**（Firefox 不支援本機模式）。

1. 到專案資料夾（`sustainable-mans-official`），**點兩下 `開啟官網後台.bat`**
2. 會跳出兩個黑色視窗（一個是說明、一個是網站伺服器），**都不要關**
3. 約 10 秒後瀏覽器自動開啟後台頁面
   （本機後台的網址是 http://localhost:4321/admin/index.html ）
4. 點藍色按鈕「**Work with Local Repository**」（使用本機儲存庫）
5. 選擇專案資料夾 `sustainable-mans-official`，按「檢視檔案」允許存取
6. 開始編輯！存檔會**直接寫進你電腦裡的檔案**
7. 想預覽改完的樣子：瀏覽器另開分頁進 http://localhost:4321/ 就是本機版官網
8. 用完把兩個黑色視窗關掉即可

**本機改完，怎麼讓它上線？**
本機模式只改你電腦裡的檔案，還沒上線。上線需要把變更推送（push）到 GitHub：
- 最簡單：請 AI 助手（Claude）幫忙「把我剛剛在後台改的內容 commit 並 push 上線」
- 或自己用 GitHub Desktop / git 指令 commit + push 到 `main`

---

## 四、後台裡有哪些內容可以編？

| 選單 | 對應內容 | 備註 |
|---|---|---|
| 📰 知識專刊文章 | 官網「知識專刊」的每篇文章 | 可新增/刪除；「設為精選」會放到專刊頁最上方 |
| 🌿 永續服務 | 永續翼的服務（碳盤查、碳足跡、桌遊、綠色策展、MS-RDK） | 含特色、重點數據、FAQ |
| 💻 數位服務 | 數位翼的服務（AI 導入、數位建置、企業自動化） | 欄位與永續服務相同 |
| 👥 專業顧問團隊 | 團隊成員的姓名、職稱、照片、專長 | |
| 🌱 行動紀錄 | 活動照片牆 | 日期格式：2025.06.27 |
| 🤝 合作夥伴 | 夥伴名稱與 Logo | |
| ⚙️ 公司基本設定 | 公司名稱、標語、聯絡資訊、社群連結、頁尾 | 影響全站，改之前想清楚 |

每個欄位下方都有灰色小字提示與範例值，照著填就不會錯。

---

## 五、常見問題（FAQ）

**Q1：儲存時跳出錯誤怎麼辦？**
先檢查是不是有「必填欄位」還空著（會標紅色）。仍失敗的話，把畫面截圖傳給工程協助。

**Q2：上傳的圖片會放到哪裡？**
後台上傳的圖片會存到 `public/images/uploads/`，網址是 `/images/uploads/檔名`。
建議上傳前先把檔名改成英文或數字，避免特殊符號。

⚡ **上傳前請先把圖片壓小**：官網 2026 年 7 月做過全站圖片瘦身（全部轉為 WebP、
單張不超過 300KB）。手機直出照片動輒 3~5MB，直接上傳會拖慢整個網站。
最簡單的做法：上傳一批新圖後，請 AI 助手「跑一次圖片優化腳本
`node scripts/optimize-images.mjs` 再上線」。

**Q3：「網址代號 (slug)」可以改嗎？**
盡量不要。它是服務內頁的網址（例如 `/green/services/iso14064/`、`/digital/services/ai-consulting/`），
改了會讓舊連結失效、也可能影響 Google 收錄。真的要改請連同工程一起處理轉址。

**Q4：本機後台按鈕上找不到「Work with Local Repository」？**
確認你是用 Chrome 或 Edge 開 `http://localhost:4321/admin/index.html`（網址開頭必須是 localhost）。
Firefox、Safari 不支援本機模式。

**Q5：線上後台登入一直失敗？**
確認 GitHub 帳號有倉庫權限；瀏覽器不要擋彈出視窗（登入視窗是 popup）。
清除 cookie 後重試一次，仍失敗就找工程檢查 OAuth 設定。

**Q6：不小心把內容改壞了怎麼辦？**
所有變更都有 Git 紀錄，任何時間點都能還原。把「大概什麼時候、改了哪個區塊」
告訴工程或 AI 助手即可救回，不用緊張。

**Q7：為什麼後台選單有些欄位標「選填」？**
選填欄位留空不影響儲存；但**請不要刪掉已有內容的欄位值**，除非你確定官網上
不需要顯示它（例如某服務不想放 FAQ）。

---

## 六、給工程／AI 的技術備忘

- CMS：Sveltia CMS（CDN：`https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js`）
- 進入點：`public/admin/index.html`；設定：`public/admin/config.yml`（Decap 相容格式）
- 後端：GitHub `sustainablemans-website/officialwebsite`，branch `main`
- OAuth：`base_url: https://www.sustainablemans.com` + `auth_endpoint: api/auth`
  （Vercel serverless：`api/auth.js`、`api/callback.js`，勿動）
- 本機模式：Sveltia 原生 File System Access API，**不需要** `local_backend` 或 proxy
- `partners.json` 最外層是陣列，config 用 Sveltia 專屬的 `root: true` 處理
- ⚠️ config.yml 欄位必須與 `src/data/*.json`、`src/content/articles/*.md`
  實際欄位 100% 對齊，缺欄位＝後台存檔會刪資料
